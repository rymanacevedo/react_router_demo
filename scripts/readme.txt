This program finds all .properties files in the target directory, and matches them to other property files in the same directory to find if translations are missing.

To run the program,
make sure identify-missing-properties.sh and identify-missing-properties.py are present
usage:
identify-missing-properties.sh  <target> <archive name>
